/var/log/mattermost-${appconfig.appconfigid}/*log {
   missingok
   sharedscripts
   postrotate
      /usr/bin/systemctl reload mattermost@${appconfig.appconfigid}.service 2>/dev/null || true
   endscript
}
